#include<stdio.h>
int main(){
    int n,a[100],i;
    printf("enter size:");
    scanf("%d",&n);
    for(int i=0;i<n;i++);
    scanf("%d",&a[i]);
    printf("array:");
    for(int i=0;i<n;i++)
    printf("%d",a[i]);
    return 0;
    
}
//logic behind the code 
take size of array (n)
input n elements
use a loop to display all elements